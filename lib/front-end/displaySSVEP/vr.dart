import 'package:flutter/material.dart';

class VR extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('VR Page')),
      body: Center(
        child: Text('hi', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
